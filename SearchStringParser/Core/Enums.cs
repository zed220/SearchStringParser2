namespace SearchStringParser {
    public enum SearchMode {
        Like,
        Contains,
        Equals,
        StartsWith,
        EndsWith
    }
    public enum SearchModificator {
        None, Include, Exclude
    }
}
